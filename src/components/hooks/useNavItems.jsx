import { useAuth } from "../../lib/AuthContext";



export default function useNavItems() {
  const {session, signOut} = useAuth();

  /**
   * TodO: Return different nav items based on the user's session
   */

}